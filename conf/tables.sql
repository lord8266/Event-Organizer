CREATE TABLE users(id VARCHAR(300) PRIMARY KEY,username VARCHAR(50) NOT NULL,email VARCHAR(50) NOT NULL UNIQUE,password VARCHAR(50) NOT NULL);
CREATE TABLE events(id VARCHAR(300) PRIMARY KEY,name VARCHAR(100) NOT NULL,start VARCHAR(50),end VARCHAR(50), paid INT, price INT,tags VARCHAR(50),description VARCHAR(1000),owner VARCHAR(100) );